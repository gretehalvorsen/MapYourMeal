@model MapYourMeal.Models.Restaurant 

<div class="container mt-4">
    <div class="row">
        <!-- Restaurant Details and Reviews -->
        <div class="col-md-6">
            <!-- Display restaurant details -->
            <h1 class="fw-bold">@Model.RestaurantName</h1>
            <p>@Model.Address, @Model.City</p>
            
            <!-- If there are reviews, display them -->
            <h3>Anmeldelser</h3>
            @if (Model.Reviews != null && Model.Reviews.Any())
            {
                @foreach (var review in Model.Reviews)
                {
                    <div class="review-list mb-4 p-3 border rounded">
                        <p>@review.Note</p>
                        <span>Rating: @review.Rating</span>
                        <p><strong>Written by: </strong>@(review.User?.UserName ?? "Unknown user")</p>
                        <p><strong>Date: </strong>@review.CreatedDate.ToString("MMMM dd, yyyy")</p>
                    </div>
                }
            }
            else
            {
                <p>No reviews available.</p>
            }

            <!-- Register new review Button -->
            <div class="text-end">
                <a href="@Url.Action("CreateReview", "Restaurant", new { restaurantId = Model.RestaurantId })" 
                class="btn btn-secondary" 
                style="background-color: #2a5b80;">Register new review</a>
            </div>
        </div>

        <!-- Map -->
        <div class="col-md-6">
            <div id="map" style="height: 50vh; width: 100%;"></div>
        </div>
    </div>
</div>

<a href="@Url.Action("Index", "Home")" class="btn btn-success mt-4">Back to all restaurants</a>